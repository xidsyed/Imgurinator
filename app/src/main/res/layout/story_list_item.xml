<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="75dp"
    android:layout_marginVertical="10dp"
    android:layout_height="wrap_content">


    <com.google.android.material.card.MaterialCardView
        android:id="@+id/story_list_item"
        android:layout_marginTop="5dp"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:elevation="2dp"
        app:cardCornerRadius="60dp"
        app:strokeColor="@color/black"
        >

        <ImageView
            android:id="@+id/story_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            tools:background="@android:color/holo_blue_dark"/>

    </com.google.android.material.card.MaterialCardView>
        <TextView
            android:id="@+id/story_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Awww"
            app:layout_constraintTop_toBottomOf="@id/story_list_item"
            app:layout_constraintStart_toStartOf="@id/story_list_item"
            app:layout_constraintEnd_toEndOf="@id/story_list_item"
            android:textAlignment="center"
            android:singleLine="true"
            />

</androidx.constraintlayout.widget.ConstraintLayout>